export enum ERoute {
    LOGIN = "/login",
    USERS = "/users",
    NAVIGATION = "/navigation",
    PAGE = "/page",
    BLOCK = "/block",
    ADMIN = "/admin",
    HEADER = "/header",
    FOOTER = "/footer",
    CONTACT = "/contact",
    PROFILE = "/profile",
    FILTER = "/filter",
}
